# Samples

Place bell WAV files here for analysis. Files in this directory are not tracked by Git.

Run the analyzer with:

```bash
python analyze_bell.py samples/your_bell.wav --format table
```

Synthetic test fixtures are generated automatically by `tests/generate_test_sample.py` during test runs.
